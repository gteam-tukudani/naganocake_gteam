<h1>ショッピングカート（カート内一覧画面）</h1>

<table>
  <thead>
      <th>商品名</th>
      <th>単価(税込)</th>
      <th>数量</th>
      <th>小計</th>
  </thead>
  <tbody>
    <%= link_to "カートを空にする", cart_items_destroy_all_path, method: :delete, class: "btn btn-danger btn-sm" %>
  <% @cart_items.each do |cart_item| %>
      <tr>
          <td>
             <% if cart_item.item.image.attached? %>
        <%= image_tag cart_item.item.image, size: "50x50" %>
      <% else %>
        <%= image_tag "no_image.jpg", size: "50x50" %>
      <% end %>
             <%= cart_item.item.name %>
          </td>
          <td><%= cart_item.item.with_tax_price %></td>
          <td>
             <%= form_with model:cart_item, url:cart_item_path(cart_item.id), local:true do |f| %>
               <%= f.select :amount, *[1..10] %>
               <%= f.submit "変更" %>
             <% end %>
          </td>
          <td><%= cart_item.subtotal %></td>
          <td><%= link_to "削除", cart_item_path(cart_item), method: :delete, data: { confirm: "商品を削除しますか？" } %></td>
      </tr>
  <% end %>
    <%= link_to "買い物を続ける", root_path, class: "btn btn-danger btn-sm" %>
    <%= link_to "情報入力に進む", new_order_path, class: "btn btn-danger btn-sm" %>
  </tbody>
</table>